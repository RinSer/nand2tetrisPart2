/** Initializes a new Zombie game and starts running it. */
class Main {
    function void main() {
        var ZombieGame game;
        let game = ZombieGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
